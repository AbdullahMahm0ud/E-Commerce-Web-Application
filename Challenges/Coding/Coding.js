var timeRequiredToBuy = function(tickets, k) {
    let queue = [];
    // fill queue with 0,1,2,...,n-1

    let remaining = [...tickets];   // copy
    let seconds = 0;

    while (true) {
        console.log(...tickets)
        // seconds++
        // buy 1 ticket
        // if this is k and remaining == 0 → return seconds
        // else if still has tickets → push back to queue
    }
};


let ans = timeRequiredToBuy(tickets = [2,3,2], k = 2)